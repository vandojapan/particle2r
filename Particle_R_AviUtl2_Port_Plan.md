# パーティクル(R) ver3.54B：DLL静的解析とAviUtl2移植計画

調査日：2026-09-18  
対象：添付「パーティクル(R).zip」／`particle_ver3_54B/particle_set3.dll`  
指定技術：Rust、aviutl2-rs（Cargo上のクレート名は `aviutl2`）

## 1. 結論と今回の到達点

**Rustで再実装するための構造・設定インターフェース・一部の数値処理を復元できた。推奨構成は、ホスト非依存のRust計算コア＋aviutl2-rsを用いるネイティブフィルタ `.auf2`。** 元のオブジェクトを粒子化する使い方を先に実装し、独立したカスタムオブジェクトは後から同じコアで追加する。

旧DLLは32bit Lua 5.1用であり、64bitのAviUtl2へそのままロードする構成にはしない。拡張子の変更、エクスポート名の変更、RustのFFIラッパーだけでは移植できない。旧DLLを別プロセスで呼ぶ方式は、画像転送と旧ホスト依存を残すため採用しない。

本調査は「文字列を抽出して終わり」ではなく、PEのエクスポート／インポート、Lua登録テーブル、設定スクリプト32項目、描画コールバックの保存、MT19937乱数処理、シード混合、頻度・精度・時間換算まで逆アセンブルと照合した。ただし、**DLL全体をC/Rustソースへ完全復元したものではない。衝突・空間場・多数の挙動・描画順の全分岐は未復元**である。

今回未実施：旧AviUtlでのDLL実行、AviUtl2実機ロード、画像差分、Rustビルド、速度測定。成果物は実装前の解析・設計であり、完成した移植プラグインではない。

### 証拠の区分

- **A：バイナリ確認** — PE構造、逆アセンブル、参照先の実データで確認。
- **B：配布資料確認** — 同梱 `.anm`、説明書、サンプルから確認。実行時の全挙動を保証しない。
- **C：移植設計** — 今回提案する実装方針。旧実装の復元結果と区別する。
- **U：未確定** — 追加逆解析またはWindows実機比較が必要。

## 2. 入力ファイルと調査方法

| 項目 | 結果 |
|---|---|
| ZIP SHA-256 | `df0c99747a3aa67ae82242fa50aa7e159158588cd0dd40765c2c235b42c2e80b` |
| DLL SHA-256 | `4a7b00013f819c83bc4f908d4b3f76c0cc469f5db795b6b0de127b9a74d51664` |
| DLLサイズ | 702,990 bytes |
| 全ファイル数 | 185（ディレクトリを除く） |
| 構成 | TXT 78、EXA 56、AUP 22、EXO 11、PNG 10、ANM 3、EXE 3、DLL 1、INI 1 |
| 配布版 | フォルダ名と履歴がver3.54B。資料内の最終バージョン変更記述は2015-05-16 |
| 原作者表記 | rikky |
| 旧対応環境 | 同梱説明ではAviUtl 1.00＋拡張編集0.92 |

ZIPの日本語ファイル名はUTF-8フラグがないエントリについてCP437経由の生バイトをCP932として復元した。テキストはCP932をUTF-8へ変換して調査。元ファイルは改変せず、DLLとEXEは実行していない。

GNU objdumpによるPE情報・x86逆アセンブルと、Python標準ライブラリによる登録テーブル・設定の抽出を使用。PDBもソースコードも添付されていない。PEのシンボル／デバッグ情報は除去済み。PEタイムスタンプは1970年相当であり、制作日の根拠にはしない。

再現用スクリプト、設定一覧、ファイルハッシュ、注釈付き逆アセンブルを別添 `Particle_R_Analysis_Evidence.zip` に収録した。

## 3. DLLの構造

### 3.1 ABIと依存関係【A】

- PE32、Machine `IMAGE_FILE_MACHINE_I386`、32bit DLL。
- ImageBase：`0x6B700000`。
- PE公開関数は1個：`luaopen_particle_set3`、RVA `0xA25E0`。
- Lua C APIは `lua51.dll` の25関数へ静的インポート。
- その他のインポート先は `kernel32.dll`、`msvcrt.dll`、`user32.dll`。
- `luaL_register` を用いて `particle_set3` というLuaライブラリを登録する。
- `GetFilterTable` 等のAviUtlフィルタプラグイン入口ではない。
- `exedit.auf`、Direct3D、OpenGLは静的インポート一覧にない。ただし動的取得やホスト経由の処理がないことを意味しない。

`libgcc_s_dw2-1.dll` などの文字列はランタイム由来の参照候補として存在するが、静的インポートDLLと同列に「必須依存」とは扱わない。

### 3.2 Lua登録テーブルを復元【A＋B】

`luaopen_particle_set3` は登録テーブルVA `0x6B7A5000` とライブラリ名を `luaL_register` に渡し、1を返す。テーブルは6組の「関数名ポインタ・関数ポインタ」とNULL終端。

以下のVAは解析時ImageBaseでの値。ASLR／別ロード位置ではRVAを使用する。

| Lua関数 | RVA | VA | スクリプトからの呼び出しと用途 |
|---|---:|---:|---|
| `concenfunc` | `0x1280` | `0x6B701280` | 集結点設定。`pconset,pconset2` の2値を受け取る |
| `gridfunc` | `0x2710` | `0x6B702710` | グリッド情報。`pgridinfo` に受け取る |
| `linefunc` | `0x3080` | `0x6B703080` | 出力位置・ライン等。`plineset` に受け取る |
| `windfunc` | `0x54D0` | `0x6B7054D0` | 風設定。`pwindset` に受け取る |
| `timefunc` | `0x8430` | `0x6B708430` | 時間グラフ設定。`ptimeset2` に受け取る |
| `func` | `0x31DD0` | `0x6B731DD0` | パーティクル本体のメイン処理 |

呼び出し元は全て `particle_set3.xxx()` という明示引数なしの形。実際の入力はLuaグローバル変数と `obj` から読む。戻り値の受け取り方はスクリプト側で確認しており、全分岐での返却テーブル形状の復元は未完了。

### 3.3 旧実装の責務分担【A＋B】

1. 各アニメーション効果がトラックバー、チェックボックス、ダイアログを提供する。
2. `protset`、`pzoalset`、`preboundset` 等の位置依存Lua配列へ設定を詰める。
3. 同一オブジェクトでは原則として最下段の「パーティクル本体」が `func()` を呼ぶ。
4. DLLが設定を収集し、必要なホスト情報・画像・カーブを取得して計算する。
5. DLLはLuaを介してホストの描画関数を呼ぶ。

`func` のRVA `0x32895`〜`0x32B20`付近では、`obj.draw`、`obj.drawpoly`、`obj.getvalue`、`obj.copybuffer`、`obj.setoption`、`obj.getoption`、`obj.load`、`obj.setfont`、`obj.effect`、`obj.getpixeldata`、`obj.interpolation` を `pdraw`、`pdrawpoly` 等のグローバルへ保存する処理を確認した。

したがって本DLLは「位置だけ計算する純粋なエンジン」ではない。ホストAPI呼び出し、素材読込、画素参照、フィルタ適用、描画まで混在したエンジンである。

## 4. 復元できた数値仕様と注意点

### 4.1 基本設定【A＋B】

| 設定 | 初期値／意味 | 移植で守ること |
|---|---|---|
| 出力速度 | 100。資料上は1秒間の移動距離 | 単位を表示し、旧値を保持 |
| 出力頻度 | 100。10で毎秒1回、100で毎秒10回 | 回数/秒は旧値÷10。間隔は10÷旧値 |
| 出力方向 | 0度で画面下 | 座標系を一般的な数学座標へ無断変更しない |
| 拡散角度 | 60。180で全方位 | 全角／半角と乱数の分布を実機で照合 |
| 同時発生数 | `sync=0` | 0の境界意味、加算方式はU。0＝粒子ゼロと決めつけない |
| 生存時間 | `ju=3`秒 | 寿命境界の含む／含まないはU |
| 透過率 | `{100,50}` | 旧名称は実質不透明度。100が不透明、0が透明 |
| 拡大率 | `{100,100}` | 百分率を内部の倍率へ明示変換 |
| 開始時間 | `tsub=0`秒 | 発生を遅らせるだけではなく、資料では経過状態の前倒し |
| 描写精度 | スクリプト既定2 | DLLは数値を整数化、最低1へ補正 |
| シード | `ran=0` | 負値でレイヤーによる変化を抑える仕様 |
| 平面描写 | 無効、領域1280×720 | 直接描画と一枚画像への合成を区別 |

### 4.2 逆アセンブルから確認した変換【A】

- **頻度**：RVA `0x32700`〜`0x32725`、定数VA `0x6B7A75EC`のfloat値は10。`10 / track1` を保存する。
- **精度**：RVA `0x730BF`〜`0x73121`で整数化して最低1に補正。RVA `0x32003`〜`0x32031`でその逆数を作る。
- **フレームレート**：RVA `0x325C3`〜`0x32641`で `obj.framerate` を整数化した値とその逆数を保持する。旧版の29.97等での誤差が考えられるため、単なるRustの精度改善と互換動作を分ける。
- **加速度**：RVA `0x320BA`〜`0x320E1`で `ac` にdouble定数0.01を掛けて保存する。これを最終的な物理加速度そのものと断定せず、後続の積分倍率を追う。
- **方向**：RVA `0x32743`〜`0x3276C`で `track2` の符号を反転し、約π/180を掛けている。
- **寿命省略時**：RVA `0x31E88`でfloat定数3.0を使用。

精度・fpsを使った後続の倍率は、全ての物理更新と結び付けての検証がまだ必要。たとえば「描写精度＝単純なサブステップ数」とだけ解釈して実装を確定しない。

### 4.3 乱数エンジンとシード【A：高確度】

DLL内にMT19937の特徴を持つ実装を確認した。

| 処理／データ | RVA／VA | 根拠 |
|---|---|---|
| 単一シード初期化 | RVA `0xACB0` | 624要素、`1812433253 * (x ^ (x >> 30)) + i` |
| 配列シード初期化 | RVA `0xACF0` | 初期値19650218、乗数1664525／1566083941 |
| 次の乱数生成 | RVA `0xAE10` | twist更新、11/7/15/18ビットのtempering |
| 状態配列 | VA `0x6B7A9020` | 624個の32bit値を使用 |
| 状態位置 | VA `0x6B7A5038` | 624／625を含む状態管理 |
| twist用テーブル | VA `0x6B7A757C` | `0x00000000, 0x9908B0DF` |
| temperingマスク | RVA `0xAF09`, `0xAF15` | `0x9D2C5680`, `0xEFC60000` |

標準的なRust `rand` の既定RNGで置換すると乱数列が変わる。互換モードでは32bitのwrapping算術でこの系列を維持する。

メイン初期化のRVA `0x33C57`〜`0x33CBF`では、概ね次の式が復元できる。`r` は整数化した指定シード、`L` は指定シードが非負ならホストのレイヤー整数値、負なら0。演算は下位32bitで扱う。

```text
mixed = 0x05E30A6D * L^3
      + 0x00871E4B * L * r
      + 0x077A5195 * r^3
initial_seed = mixed + 0x000181DD    (mod 2^32)
```

この値でMT状態を初期化する経路をRVA `0x33D09`〜`0x33D60`で確認。ホストのレイヤー番号の起算、異なる機能内での再シード、粒子ごとの乱数消費順、整数／実数範囲への変換は、引き続き復元が必要である。**MT19937だけ一致しても画像全体は一致しない。**

旧状態配列はDLLグローバル。Rust版でそのまま共有すると複数オブジェクトや並列処理で混線するため、計算インスタンスごとに所有させ、互換な再シード／消費順をその中で再現する。

### 4.4 同梱スクリプトにある不整合候補【B＋U】

「音」はダイアログで `subtime` を定義する一方、`paudset` の6番目に `subt` を渡している（主スクリプト548行・597行・600行）。このセクション内には `subt` の定義がなく、設定名不一致の候補。旧環境での実際の影響は未検証。移植時には意図した開始ずれとして直すか、旧設定互換として扱うかを比較ログとともに決める。

「前後表裏その他切替」の時間短縮チェックはUI自身に未使用と書かれ、`pswtset` へ `obj.check0` が渡されない。これを移植必須の完成機能として数えない。

## 5. 全32項目の移植マトリクス

段階：P2＝基本版、P3＝運動拡張、P4＝素材／描画／連携、P5＝拡張互換。難易度は今回の設計評価であり実装済みの評価ではない。全トラックバーの範囲・既定値・ダイアログ定義は別添の `parameters.json` と `Parameter_Catalog.md` に収録。

| # | 旧項目 | 受け渡し／責務 | 段階 | 難度・課題 |
|---:|---|---|---|---|
| 1 | パーティクル本体 | `func()`、基本発生・寿命・描画 | P2 | 高：乱数と時間の基準 |
| 2 | パス | `particle_path`、番号1〜10とファイル | P2 | 低：相対パス・文字コード |
| 3 | 前後表裏その他切替 | `pswtset`、順序・裏面・回転・逆再生等 | P3/P5 | 高：描画順。ゲームはP5任意 |
| 4 | 方向と格子 | `pzgridset`＋`gridfunc()` | P3/P4 | 高：3D方向と画素由来グリッド |
| 5 | 出力 | `linefunc()`／`plineset`、8種の発生位置 | P2/P4/P5 | 高：P2は点・線・箱・球。レイヤー追跡P4、関数P5 |
| 6 | 回転 | `protset`、揺れ・回転加速度 | P3 | 中：角度単位と時間基準 |
| 7 | 集結点 | `concenfunc()`、最大5アンカー | P3 | 高：集結・消滅・停止 |
| 8 | 拡大率と透過率 | `pzoalset`、中点・補間・個別変化 | P2/P3 | 中：基本線形から拡張 |
| 9 | フィルター | `peffectset`、最大10設定 | P4 | 高：効果名／引数変換と順序 |
| 10 | フィルター(単色化専用) | `pcolset`、色とランダム変化 | P3/P4 | 中：色空間と輝度保持 |
| 11 | 個別微調整 | `pkobetsuset`、粒子別ばらつき | P3 | 高：RNG消費の互換 |
| 12 | 時間 | `ptimeset`、13系列の周期変化 | P3 | 中：出生時／現在時の評価 |
| 13 | 時間2 | `timefunc()`／`ptimeset2`、15系列 | P3/P4 | 高：別オブジェクトのグラフ |
| 14 | 跳ね返り | `preboundset`、軸境界と球 | P3 | 高：反射・境界・反発係数 |
| 15 | 跳ね返り2 | `preobjectset`、他レイヤー領域 | P4 | 高：画像マスク・時間変化 |
| 16 | テキスト | `pfontset`、文字／行別出力 | P4 | 高：日本語分割・フォント描画 |
| 17 | 画像 | `pimageset`、図形・画像選択 | P2/P4 | 中：基本素材P2、抽出・背景P4 |
| 18 | 動画 | `pmobieset`、動画・連番・キー処理 | P4 | 高：粒子別時刻・素材キャッシュ |
| 19 | 風 | `windfunc()`／`pwindset` | P3/P4 | 高：別オブジェクトと空気抵抗 |
| 20 | 挙動 | `pactset`、パターン・ゆらぎ・自作関数 | P3/P5 | 高：パターン0〜101の分岐 |
| 21 | 挙動2 | `pbehaset`、分散・停止・イベント同期 | P3 | 高：反射とのイベント順序 |
| 22 | 挙動3 | `pcirmset`、円／球運動と移動中心 | P3/P4 | 高：全期間のカーブ参照 |
| 23 | 挙動4 | `pacttimeset`、放射／活動時間・時間変形 | P3 | 高：逆方向評価・連続性 |
| 24 | 空間場 | `pfieldset`、空間乱数による力場 | P3 | 高：ノイズ／場の式が未復元 |
| 25 | 軌跡 | `ptrackset`／`pvanish`、3種の軌跡 | P3 | 高：履歴・寿命後の残存 |
| 26 | ファンネル | `pfanset`、子粒子・円環・同期 | P4 | 高：多段変換と増殖数 |
| 27 | メッシュ | `pmeshset`、粒子間の線／面 | P4 | 高：接続順と透明合成 |
| 28 | 立体物 | `pcubeset`、7タイプ、PMDを含む | P4/P5 | 高：頂点・面・材質とモデル読込 |
| 29 | ガラス化(二次元) | `pglaset`、背景参照・縁・ぼかし | P4 | 高：フレームバッファと合成順 |
| 30 | ユーザーカスタムオブジェクト | `pobjset`、Lua外部定義・引数 | P5 | 高：ホストAPI依存の互換実行 |
| 31 | 他作アニメーション効果オプション | `anmopset`、外部アニメーション連携 | P5 | 高：任意Luaと効果の依存関係 |
| 32 | 音 | `paudset`／`roku`、10帯域スペクトル | P4 | 高：音取得・記録と任意シーク |

旧「風」「時間2」は説明書・DLLのメッセージから別オブジェクト／上位レイヤーに置く使い方がある。全てを「同一オブジェクトの前段設定」とみなす移植は誤り。`throughf` の伝播・消費仕様も別途扱う。

## 6. AviUtl2・aviutl2-rsの調査結果

### 6.1 固定する基準

調査時の公式配布ページはAviUtl ExEdit2 **2.1.9（2026-09-12）**、SDK配布日2026-09-05。対象Windowsはユーザー環境を想定してWindows 11 x64とする。[公式配布ページ](https://spring-fragrance.mints.ne.jp/aviutl/)

aviutl2-rsのmainを調査時に取得し、GitHub APIでcommit **`b38f65c06790c0380036435a5df4d55a0cc294b9`** を記録。Cargo workspaceのversionは **0.46.1**、editionは2024。クレート名は `aviutl2` であって `aviutl2-rs` ではない。[Cargo.toml](https://github.com/sevenc-nanashi/aviutl2-rs/blob/b38f65c06790c0380036435a5df4d55a0cc294b9/Cargo.toml)

docs.rsの `/latest/` ではページによって0.42.0／0.43.0／0.46.1の表示が混在したため、設計上の具体的APIは取得したソースを優先した。0.46.1と本体2.1.9の組み合わせを実機認定済みという意味ではない。P0で必要APIの動作とSDK対応版を確定し、Cargo.lock・Rust toolchain・ホスト版を固定する。

### 6.2 ソースで存在を確認したAPI

| 旧DLLの責務 | aviutl2-rs側の候補 | 対応の限界 |
|---|---|---|
| オブジェクトを粒子として描く | `FilterProcVideo::draw_image`, `DrawImageParam` | 座標・回転順・alphaの一致は実機照合 |
| 四角形／三角形・メッシュ | `draw_poly`, `VertexList` | 透明度順・裏面・カメラは要照合 |
| 平面描写 | `create_image_resource`, `draw_image_to_resource`, `copy_image_resource` | 二重変換と後段効果を検証 |
| 画素読込 | `get_image_data`, `get_image_resource_data` | 旧画素形式を直接流用しない |
| レイヤー追跡 | `get_image_object`, `get_output_image_param` | 旧getvalueは生トラック値、候補APIは出力パラメータ。意味が同一とは限らない |
| 粒子別フィルタ | `exec_effect` | alias形式の名前・値。旧 `obj.effect` の引数を無変換で渡せない |
| 裏面／正面制御 | `set_culling_state`, `set_billboard_mode` | カメラを見る＋回転の組合せを確認 |
| 任意画像への効果実行 | `exec_effect`＋`ImageResource::Resource` | 効果が対応していることを個別に確認 |
| 直接描画後の重複出力抑止 | `prevent_post_effect` | 後段処理を中断する。平面描写では使い分け |
| LuaからRust呼び出し | `ScriptModule`, `module::functions`, `register_script_module!` | `.mod2`。旧lua_Stateと同じABIではない |
| エフェクト固有設定保存 | ConfigData／可変長ConfigData | 永続設定と計算キャッシュを分離 |

根拠：[FilterProcVideo実装](https://github.com/sevenc-nanashi/aviutl2-rs/blob/b38f65c06790c0380036435a5df4d55a0cc294b9/crates/aviutl2/src/filter/binding/video.rs)、[filter2バインディング](https://github.com/sevenc-nanashi/aviutl2-rs/blob/b38f65c06790c0380036435a5df4d55a0cc294b9/crates/aviutl2-sys/src/filter2.rs)、[スクリプト引数API](https://github.com/sevenc-nanashi/aviutl2-rs/blob/b38f65c06790c0380036435a5df4d55a0cc294b9/crates/aviutl2/src/module/param.rs)。

`ScriptModuleCallHandle` には引数・配列・返却値に加えて `read_section()` もある。ただし旧DLLのように生のLuaスタックから任意グローバルを読む設計ではない。旧 `func()` を引数なしで移しただけでは、旧32項目の設定は受け取れない。

## 7. 採用する移植構成【C】

### 7.1 方式比較

| 方式 | 評価 | 用途 |
|---|---|---|
| Rust `.auf2`＋純Rust core | **主案** | 元オブジェクト画像を入力にし、計算・描画・設定を管理する |
| `.anm2`＋Rust `.mod2` | 互換UIの補助案 | 旧スクリプトの操作感を残す必要が強い場合。設定を明示的に受け渡す |
| `.obj2`＋Rust `.mod2` | 後続案 | 元画像を必要としない専用の発生器 |
| `.aux2`のみ | 本体として不採用 | 将来のプリセット編集・移行UIには利用可能 |
| 旧32bit DLLのブリッジ | 不採用 | 旧ホスト・Lua ABI・画像転送を残す |

`.auf2` は単に静止画を作るだけでなく、現行APIで頂点描画やホスト効果も扱える。まず一つのエフェクトとして基本機能を完成させる。旧32効果をそのまま32個のRustプラグインに分割すると、共有設定の評価順・保存・並列処理が複雑になるため、初期版は項目グループを持つ一つの設定に統合する。

旧効果の積層を忠実に残すことが必須になった場合のみ、P0で `.anm2＋.mod2` を別アダプターとして検証する。どちらのUIでも同じ `ParticleConfig` とコアを利用し、エンジンを二重実装しない。

### 7.2 リポジトリ構成案

| パス | 責務 |
|---|---|
| `crates/particle-core/` | 型、乱数、発生、運動、寿命、イベント、描画用データ生成 |
| `crates/particle-aviutl2/` | aviutl2クレート経由の `.auf2`、設定UI、ホスト描画・素材取得 |
| `crates/particle-legacy-import/` | EXA/EXO読込、CP932、設定の正規化と互換診断 |
| `crates/particle-module/` | 必要になった場合のみ `.mod2` アダプター |
| `tools/reference/` | 旧AviUtlでの基準データ取得と比較手順 |
| `tests/fixtures/` | 設定、期待する粒子データ、許諾・出所を管理した比較画像 |
| `docs/compatibility/` | 32項目の対応状況、未知仕様、旧版との差 |

ビルドターゲットは `x86_64-pc-windows-msvc`。ホストアダプターは `cdylib`。初期候補の依存定義は次の形とし、実際のビルドでfeature依存とMSRVを確認する。

```toml
[lib]
crate-type = ["cdylib"]

[dependencies]
aviutl2 = { version = "=0.46.1", default-features = false, features = ["filter", "generic", "serde"] }
```

Cargo上の依存指定例であり、この調査でコンパイル確認したコードではない。`particle-core` はWindowsやaviutl2への依存を持たせない。

### 7.3 主要データ型

- `ParticleConfig`：バージョン付き永続設定。旧の位置依存Lua配列を型付き構造へ変換。
- `EmitterConfig`：出力位置、頻度、同時発生数、方向、素材。
- `MotionConfig`：重力、加速度、回転、風、集結、反射、場。
- `AppearanceConfig`：不透明度、拡大率、色、裏表、軌跡・子粒子。
- `TimeContext`：scene frame、object frame、fpsの分子／分母、出生時刻、粒子年齢、変形後時刻。
- `HostSnapshot`：必要なカーブ・他オブジェクト情報・画像マスク・スペクトル。ホストが有効なコールバック中に取得した所有データ。
- `SimulationContext`：専有RNG、有限のキャッシュ、互換モード情報。
- `ParticleSample`：粒子ID、出生時刻、位置、回転、倍率、alpha、素材ID、描画順キー。
- `RenderBatch`：画像インスタンス／線／面の所有データ。ホストの生ポインタを保存しない。

`particle-core` はホストの `obj` やLuaグローバルを参照しない。ホストAPIは原則ホストコールバックのスレッド内で使い、CPU並列化を導入する際も所有済みデータだけをワーカーへ渡す。

### 7.4 描画モードを分ける

**直接描画**：粒子ごとの3D位置・回転でホストに描く。旧版の通常描写に対応。元オブジェクトが最後に重複して描画されないよう `prevent_post_effect` の挙動をP0で確認する。後段効果を自動で粒子全体へ適用するとは約束しない。

**平面描写**：透明リソースへ全粒子を合成し、Objectへ戻す。旧 `heymen` に対応。後段効果はその一枚の画像にかかる。領域外の切れ、中心座標、カメラ変換の二重適用を確認する。

素材は最初にObjectから専用リソースへ退避する。描画途中の加工画像を次粒子の元画像にしてしまう累積バグを防ぐ。粒子ごとのフィルタは元素材から作る一時リソースへ適用する。

### 7.5 任意シーク・並列性・保存

編集ソフトではフレームが順番に要求されるとは限らない。`tick()` を一回進めるだけのゲーム用更新方式は採らない。

- 互換優先の初期実装は必要な開始時刻から決定的に再計算する。
- 数値が一致した後にチェックポイントを追加し、途中から再計算する。
- キャッシュキーに設定revision、素材revision、fps、対象インスタンス、時間モード、互換モードを含める。
- 設定変更、Undo/Redo、コピー、オブジェクト削除、プロジェクト読込で無効化する。
- 負シードでレイヤー非依存にする場合、オブジェクトIDをRNGへ勝手に混ぜない。IDはキャッシュ分離にだけ使う。
- キャッシュやMT内部状態はプロジェクトの設定データとして保存しない。
- 粒子・軌跡・ファンネルの数には事前計算可能な上限とエラーを設ける。大量設定を黙って削減せず、品質設定との違いを表示する。

## 8. 互換方針と変換

### 8.1 互換性は別々に判定する

1. **設定互換**：旧項目を読み、値と意味を新設定へ変換できる。
2. **数値互換**：発生時刻・座標・RNG・物理挙動を基準と比較する。
3. **見た目互換**：旧AviUtlとAviUtl2の描画方式差を含め、許容差内に収まる。
4. **スクリプト互換**：自作Luaや他作者の効果が動く。最も後段の作業。
5. **プロジェクト互換**：旧AUPをそのまま開くこととは別。初期版では対応を宣言しない。

### 8.2 EXA/EXOインポート

添付には56 EXA・11 EXOがあるので、設定の移行テストに使える。最初は旧AUPを直接変換せず、旧環境でEXO/EXAへ書き出した設定を読む。

- CP932→Unicodeに変換し、元ファイルを残す。
- 効果名とブロック順を記録し、32項目を型付き設定へ正規化。
- 同じ効果が複数回ある場合、上書き／連結／上限を旧スクリプトに合わせる。
- Luaの値はスカラー、文字列、配列、nil等の必要なデータ構文だけを読む。設定取込のために任意のLuaコードを実行しない。
- `r`、`random`、空文字、nilは別々の意味として保持。
- パス番号、`pmobieset` 等の旧綴りも対応表で扱い、誤字修正で互換を壊さない。
- 未対応項目は項目名・元値・理由を移行レポートに出す。無言で捨てない。
- 保存形式には `schema_version`、旧版・元効果名・元値・未変換情報を含める。

### 8.3 外部Luaとサンプル

カスタム関数 `xyz(t)`、`xyzd(t)` は資料上、秒単位の出生時刻を引数とする。`xyzd` の角度返値はラジアンで、UIの度と混同しない。

任意の旧Luaは、独立Lua実行系をRustへ組み込んでも `obj.*` が自動的に使えるわけではない。P5では純粋な数値関数の互換を先に実装し、描画を伴うユーザーオブジェクトは別APIへ変換するか、専用スクリプトアダプターへ寄せる。DLLローダーや任意の外部プロセス実行を互換の前提にしない。

添付内のテキスト検索では、改変・再配布を包括的に許諾するライセンス条文は確認できなかった。解析と設計は進められるが、元DLL・元スクリプト・同梱素材を公開配布物へ含める判断は別途行う。再実装の配布ライセンスと依存クレートのライセンスは分けて管理する。

## 9. 実装フェーズと完了条件

### P0：ホスト接続の実証

**作業**：Rust＋aviutl2で最小 `.auf2` を作る。元画像を取得し、10個のコピーを描画。直接描画と平面描写を切替。移動・回転・カメラ・後段効果を試す。保存／読込・同一効果の複製・Undoを確認。

**完了条件**：AviUtl2 2.1.9上でロードし、10粒の位置とalphaが意図通り。元画像の二重出力なし。設定が再起動後も復元。使用するaviutl2版とSDK/APIの組合せを記録。

**分岐**：必要な通常／平面描画が再現できない場合だけ `.anm2＋.mod2` の最小プロトタイプでホスト連携を比較し、その結果でアダプターを確定する。

### P1：基準データと未確定仕様の回収

**作業**：旧AviUtl 1.00＋拡張編集0.92で提供サンプルを開く。旧DLLの描画呼び出しをデバッガ、Lua APIの記録用ラッパー等で観測し、粒子ID相当・出生・座標・回転・素材とPNGを記録。既存の基準環境が1.10等ならその違いも明記する。

**重点**：sync=0、先頭粒子の出生、寿命終端、シードの符号、29.97fps、負加速度の停止、風／時間2の対象と伝播、反射と分散の処理順。RVA別の未解明表を更新。

**完了条件**：少なくとも基本・重力・RNG・平面・反射・軌跡の6系統で、再計測可能な設定と基準データがある。未知仕様に仮決めで印を付けず、未解明は明示する。

### P2：基本版

**作業**：型付き設定、MT19937、シード混合、固定頻度、方向・拡散、寿命、基本移動、線形alpha/zoom、元オブジェクト／画像粒子、点・線・箱・球の発生を実装。

**完了条件**：固定seedで再計算して一致し、前後シークで同じフレームの結果が変わらない。基本プリセットを保存・再読込できる。旧値100の頻度を毎秒100個と誤解していない。

### P3：運動・時間・軌跡

**作業**：風の基本カーブ、回転、個別ばらつき、時間変調、集結、境界反射、分散停止、円運動、時間変形、軌跡、空間場を、1機能ずつ基準比較して追加。

**完了条件**：複数機能を組み合わせてもイベント順・シーク再現性を保つ。未知の空間場式や挙動パターンは未対応として表示し、類似のノイズを入れただけで互換完了にしない。

### P4：ホスト・素材・描画拡張

**作業**：他レイヤー追跡、画像マスク衝突、共有風／時間2の明示リンク、テキスト、動画・連番、粒子別フィルタ、ファンネル、メッシュ、基本立体、背景参照のガラス、10帯域音連携。

**完了条件**：不足素材・参照先削除・循環参照でクラッシュしない。カーブやマスクの時刻が出生時／現在時のどちらか明確。音を先頭から再生していないシークでも決定的に得られるか、事前解析が必要な旨が表示される。

### P5：互換インポートと外部拡張

**作業**：EXA/EXO変換、カスタム数値関数、ユーザーオブジェクト、他作者アニメーション、PMD等を個別対応。補助UIが必要なら `.aux2` をここで追加する。

**完了条件**：全32項目について「対応／一部／非対応」と理由を公開可能な表にする。旧AUP完全互換と誤表示しない。未使用チェックや付属ゲームは任意扱いとして明記。

### P6：性能・配布

**作業**：計算チェックポイント、素材共有、適切なバッチ化、必要ならSIMDやGPUを導入。Windows CIでビルドし、DLLを所定の `.auf2` 名でパッケージ化。GitHub Actions等のビルド成功とホスト実機成功を別の結果として残す。

**完了条件**：性能改善前後で互換テストを通過。1080p/4K、1千/1万粒でCPU時間・描画時間・メモリを測定し、軌跡／ファンネルありの負荷も示す。60fps達成は測定するまで保証しない。

各フェーズの工数はP0・P1での未知仕様の量に強く依存する。基本版と全機能互換を同じ納期で見積もらず、P2の完了時に残機能ごとの見積もりを更新する。

## 10. 検証項目

| ケース | 主な確認内容 |
|---|---|
| 静止した単一発生源 | 出生数、出生時刻、方向、寿命 |
| seed 0／1／負値＋レイヤー移動 | RNG系列、レイヤー依存、負値の扱い |
| 24／30／60／30000÷1001fps | fps整数化互換、時間ずれ、境界フレーム |
| シーク 0→120→30→120 | 同じ120フレームの数値結果が一致 |
| プレビューと書出し | 実行順やバッファの違いで結果が変わらない |
| alpha・拡大率の端点 | 100＝不透明、寿命直前・直後の処理 |
| 負加速度と重力 | 停止条件、物理単位、精度1/2/5 |
| 反射＋分散＋フィルタ | イベント処理順と乱数消費 |
| 長い軌跡＋時間変形 | 履歴復元とキャッシュ無効化 |
| カメラ＋平面／直接描画 | 二重変換、中心、裏表、後段効果 |
| 2個以上の粒子オブジェクト | 共有RNG・共有一時画像による混線なし |
| 他レイヤーの移動・削除 | 参照スコープ、無効なリンク、Undo |
| 日本語パス・欠損ファイル | Unicode、相対パス、エラーの可読性 |
| オブジェクト複製→保存→読込 | 設定の独立性と永続化 |
| 大量粒子・巨大画像 | メモリ上限、checked算術、エラー復帰 |

数値比較を画像比較より先に行う。粒子数・ID・発生順・RNG整数列は完全一致を目標にし、位置・回転は暫定絶対誤差1e-6／相対誤差1e-6で差を調べる。ただし旧x87とRust x64の浮動小数点差で閾値が分岐へ影響する場合は、単に許容値を広げずその分岐を調査する。

画像の画素完全一致は異なるレンダラー間では最初から保証しない。差分画像と目視に加え、粒子中心のずれ0.25px以下等を初期の評価候補にする。最終許容差はP1の基準データで定める。

## 11. 未確定事項と次の逆解析対象

| 優先 | 未確定事項 | 調査する場所／方法 |
|---|---|---|
| 最優先 | sync=0の意味、出生カウントと終端 | `func`初期設定・発生ループ、単一粒子の観測 |
| 最優先 | RNG再シードと実数化・消費順 | `0xACB0`／`0xAE10`とインライン展開された同等処理の参照元 |
| 最優先 | 移動の積分式、精度、fpsの結合 | `func`の初期倍率から位置更新ループまでデータフロー追跡 |
| 高 | 反射／集結／分散／軌跡の順序 | 各設定グローバルの参照と組合せ基準テスト |
| 高 | フィルタの適用順・キャッシュ状態 | `peffect`, `pdraw`, `pcopybuffer`呼出ログ |
| 高 | 風・グリッド等の消費／伝播 | `throughf`, `pwindset`, `pgridinfo`のclear/reset経路 |
| 中 | 0〜101の挙動、空間場の式 | `pactset`, `pfieldset`周辺の分岐表 |
| 中 | PMD読込と座標・材質 | `pcubeset`, ファイル読込経路、同梱説明 |
| 中 | 外部アニメーションの入力環境 | `particle_anm`, `particle_obj`, `luaL_loadfile`の参照 |
| 低 | ゲーム・変換EXEの内部 | 初期移植範囲外。必要時に別解析 |

最初の実装依頼はP0とP1までに切り、結果が揃ってからP2へ進む。全32機能を一度に生成させると、未知の数式が一般的なパーティクル式に置き換わり、互換性を評価できなくなる。

## 12. 資料と参照元

ローカル一次資料：添付ZIPの `@particle_ri_ver3.anm`、`パーティクル(R)について.txt`、`説明書/01`〜`21`と補足、EXA/EXOサンプル、および `particle_set3.dll`。

オンライン一次資料（2026-09-18確認）：

- [AviUtl公式配布・SDK](https://spring-fragrance.mints.ne.jp/aviutl/)
- [aviutl2-rs](https://github.com/sevenc-nanashi/aviutl2-rs)
- [確認commit](https://github.com/sevenc-nanashi/aviutl2-rs/tree/b38f65c06790c0380036435a5df4d55a0cc294b9)
- [ネイティブ描画サンプル](https://github.com/sevenc-nanashi/aviutl2-rs/blob/b38f65c06790c0380036435a5df4d55a0cc294b9/examples/random-color-filter/src/lib.rs)
- [mod2サンプル](https://github.com/sevenc-nanashi/aviutl2-rs/blob/b38f65c06790c0380036435a5df4d55a0cc294b9/examples/regex-module/src/lib.rs)
- [配置先を含むビルド設定](https://github.com/sevenc-nanashi/aviutl2-rs/blob/b38f65c06790c0380036435a5df4d55a0cc294b9/aviutl2.toml)

別添の証拠には元DLL・EXE・素材は同梱していない。再現スクリプトは元ZIPを入力にして解析データを再生成する。注釈付き逆アセンブルは解析の手掛かりであり、自動生成コメントだけを関数意味の証明として使わない。
